# Database Configuration
spring.application.name=Hospital Management System
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://viaduct.proxy.rlwy.net:31762/railway?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:QkQtkGMykmSokGVhLsACIAvKUdNzxWgh}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Server Configuration
server.port=${PORT:8080}

# JWT Configuration
jwt.secret=${JWT_SECRET:YOUR_SECRET_KEY_HERE_MUST_BE_VERY_LONG_FOR_SECURITY}
jwt.expiration=86400000

# CORS Configuration
cors.allowed.origins=${FRONTEND_URL:http://localhost:5173}

# Logging
logging.level.com.hms=DEBUG
logging.level.org.springframework.security=DEBUG